<%= form_with(model: moment, local: true,html: { multipart: true } ) do |form| %>
  <% if moment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(multimedium.errors.count, "error") %> prohibited this multimedium from being saved:</h2>
      <%= link_to image_tag(@post.image.url(:med)), @post.image.url %>
      <ul>
      <% moment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
       <%= collection_select(:moment, :study_id, Study.all, :id, :name, prompt: true) %>
   </div>

  <div class="field">
    <%= form.label :from %>
    <%= form.date_select :from, id: :from %>
  </div>


  <div class="field">
    <%= form.label :until %>
    <%= form.date_select :until, id: :until %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
