<%= form_with(model: wfeeling_question, local: true) do |form| %>
  <% if wfeeling_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wfeeling_question.errors.count, "error") %> prohibited this wfeeling_question from being saved:</h2>

      <ul>
      <% wfeeling_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :multimedia_id %>
    <%= form.text_field :multimedia_id, id: :wfeeling_question_multimedia_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
