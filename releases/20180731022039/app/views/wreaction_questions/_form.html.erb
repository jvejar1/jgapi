<%= form_with(model: wreaction_question, local: true) do |form| %>
  <% if wreaction_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wreaction_question.errors.count, "error") %> prohibited this wreaction_question from being saved:</h2>

      <ul>
      <% wreaction_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :multimedia_id %>
    <%= form.text_field :multimedia_id, id: :wreaction_question_multimedia_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
